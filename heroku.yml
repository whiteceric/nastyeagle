setup:
  addons:
  - plan: heroku-postgresql
build:
  docker:
    web: backend/Dockerfile
run:
  web: bash -c "gunicorn nastyeagle.wsgi:application --bind 0.0.0.0:$PORT && python manage.py makemigrations && python manage.py migrate"
